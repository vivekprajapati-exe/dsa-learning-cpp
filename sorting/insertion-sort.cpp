#include <iostream>
using namespace std;

void insertion_sort(int arr[], int n)
{
    for (int i = 0; i < n - 1; i++)
    {
        for (int j = i; j <= n - 1; j++)
        {
            if (arr[j] > arr[j - 1])
            {
            }
        }
    }
}

int main()
{

    return 0;
}