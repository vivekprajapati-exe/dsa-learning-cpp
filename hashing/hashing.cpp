#include <iostream>
using namespace std;

int hashing(int arr[])
{
}

int main()
{
    int size;
    cout << "size of arrary";
    cin >> size;
    int arr[size];
    for (int i = 0; i < size; i++)
    {
        cin >> arr[i];
    }
    hashing(arr);

    return 0;
}